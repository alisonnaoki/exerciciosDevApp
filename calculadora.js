//Exportando as Funções

export const soma = (numA, numB) => numA + numB;
export const subtracao = (numA, numB) => numA - numB;
export const multiplicacao = (numA, numB) => numA * numB;
export const divisao = (numA, numB) => numA/numB;
